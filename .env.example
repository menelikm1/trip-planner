# Copy this to .env and fill in your values
# Railway will set DATABASE_URL automatically via the Postgres plugin

DATABASE_URL=postgresql://user:password@localhost:5432/tripplanner
NODE_ENV=development
PORT=3000
